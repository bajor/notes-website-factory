name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  reusable-workflow:
    permissions:
      contents: read
    uses: ./.github/workflows/build-pdf-site.yml
    with:
      site-title: Notes Website Factory Fixture
