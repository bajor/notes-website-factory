{
  "name": "freeform-notes-generator",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsx src/cli.ts build --root .. --dist ../dist",
    "inspect": "tsx src/cli.ts inspect --root ..",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
